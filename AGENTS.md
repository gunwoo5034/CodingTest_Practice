# 프로젝트 작업 지침

이 문서는 이 저장소에서 작업하는 코딩 에이전트의 공통 규칙이다. 제품 요구사항과 구현 순서는 [PLAN.md](PLAN.md)에 둔다. Claude용 진입 문서는 [CLAUDE.md](CLAUDE.md)다.

## 현재 작업 범위

- 사용자가 사이트 구현을 요청했으므로 PLAN.md에 따른 구현·로컬 검증이 허용되어 있다. 외부 게시와 실제 유료 API 호출은 별도로 구분한다.
- 사용자가 후속 메시지에서 구현을 명시적으로 요청하면 PLAN.md의 구현 단계를 진행한다. 이미 요청한 작업을 매 단계 다시 승인받지 않는다.
- 문서에 작업 목록이 있다는 이유만으로 코드 작성, 패키지 설치, Docker 실행, 유료 API 호출, GitHub 게시를 시작하지 않는다.
- 사용자의 최신 지시를 우선한다. 요구사항이 변경되면 PLAN.md를 함께 갱신한다.

## 제품에서 지켜야 할 사항

- 지원 언어는 C++17·Python 3.12·Java 21이며 함수 반환값을 채점한다.
- 코드 실행·제출·자원 측정·자동 저장에 AI를 호출하지 않는다.
- AI는 문제 준비 및 사용자가 요청한 풀이 도움에만 사용한다.
- 개발용 서브에이전트 모델과 웹사이트의 OpenAI API 모델·인증을 구분한다.
- 사용자 코드뿐 아니라 AI 생성 코드도 격리 실행한다.
- 히든 테스트와 기준 풀이를 일반 문제 조회, 힌트 요청, 제출 결과에 노출하지 않는다.
- API 키, 문제 원문·이미지, 개인 DB, 개인 제출 기록은 GitHub 공유 대상에서 제외한다.
- 실제 확인하지 않은 테스트·플랫폼·정확성을 확인했다고 보고하지 않는다.

## 개발팀 역할과 모델 배정

아래 모델 ID는 이 설계를 작성한 Codex 환경에서 제공된 개발용 모델 배정이다. 웹사이트에서 호출할 모델은 PLAN.md의 API 정책을 따른다.

| 역할 | 담당 | 기본 모델 | 추론 강도 |
|---|---|---|---|
| 총괄 | 요구사항·공통 계약·작업 분배·통합 관리 | 현재 대화 모델 | 현재 세션 설정 |
| 프런트엔드 | web/의 UI·에디터·테스트 편집·채팅 UI | gpt-5.6-sol | medium |
| 백엔드·AI | server/의 DB·API·문제 분석·테스트 준비·도우미 | gpt-5.6-sol | medium |
| 실행기 | runner/의 언어별 래퍼·격리·자원 측정·Docker 구성 | gpt-5.6-sol | high |
| 일반 검토 | 작업별 요구사항·코드 품질 검토 | gpt-5.6-sol | medium |
| 최종 통합 검토 | 전체 흐름·인터페이스·격리·데이터 노출 검토 | gpt-6-astra | high |
| 단순 반복 작업 | 완성된 명세에 따른 작은 수정 | gpt-5.6-luna | low |

- 역할을 선언했다고 모든 에이전트를 동시에 실행하지 않는다. 필요한 작업이 있을 때만 생성한다.
- 문서에 모델 ID를 적는 것만으로 실행 모델이 설정되지는 않는다. 실제 배정 시 사용 가능한 모델과 설정을 확인한다.
- 다른 실행 도구, 특히 Claude에서는 위 Codex 모델 ID를 그대로 사용 가능하다고 가정하지 않는다. 사용 가능한 모델로 역할을 매핑하고 실제 사용 모델과 차이를 보고한다. 자격증명이나 전역 설정을 임의로 바꾸지 않는다.
- 모델별 실제 비용 절감률을 가정하지 않는다. 중복 탐색과 수정 횟수를 줄이는 것을 우선한다.

## 위임과 동시 작업

- 기본 동시 실행 한도는 총괄 포함 4명이며 실제 도구 제한이 더 작으면 그 제한을 따른다.
- 동시에 코드를 수정하는 구현 담당은 1명으로 제한한다. 다른 에이전트는 확정된 변경의 읽기 전용 검토 또는 독립적인 조사를 수행한다.
- 구현 작업을 기다려야 하는 종속 작업은 선행 계약과 결과가 확정된 뒤 배정한다.
- 공통 인터페이스·루트 설정·의존성 파일은 총괄이 지정한 한 담당자만 수정한다.
- 새 작업에는 전체 대화 이력 대신 해당 요구사항, 인터페이스, 관련 파일, 완료 조건만 제공한다. 지원되는 환경에서는 fork_turns="none"을 사용한다.
- 같은 작업의 수정은 기존 담당자를 재사용한다. 별개의 작업은 필요한 맥락만 갖는 새 담당자에게 맡긴다.
- 구현 담당은 다른 서브에이전트나 검토자를 생성하지 않는다. 위임과 검토 배정은 총괄이 수행한다.
- 독립적인 일이 없다면 슬롯을 비워 둔다. 조사만으로 해결되는 작은 문서 작업에 불필요한 팀을 만들지 않는다.

## 작업 지시서 형식

```text
목표: 이번 작업으로 완성할 사용자 동작
선행 결과: 의존하는 인터페이스와 구현 상태
읽을 자료: 요구사항 발췌, 공통 계약, 관련 파일
수정 범위: 담당 파일과 디렉터리
완료 조건: 검증할 동작과 실행 명령
제약: 범위 밖 수정·임의 API 변경·재위임 금지
보고: 상태, 변경 요약, 검사 결과, 남은 문제, 상세 기록 위치
```

- 담당자에게 저장소 전체를 반복해서 읽게 하지 않는다. 해당 작업에 필요한 공통 규칙과 파일만 지정한다.
- 한 작업은 독립적으로 검토할 수 있는 기능 단위로 묶는다. 버튼·함수마다 별도 에이전트를 만들지 않는다.
- 발견한 인터페이스 충돌은 총괄에게 보고한다. 담당자가 다른 영역까지 임의 수정해 해결하지 않는다.
- 진행·완료·보류 상태와 설계 결정을 기록해 완료된 작업을 반복하지 않는다.

## 검토와 검증

- 구현 담당이 먼저 해당 변경에 필요한 검사를 수행하고 명령·결과를 기록한다.
- 검토자는 확정된 변경을 대상으로 요구사항 충족과 코드 품질을 함께 검토한다. 버전 관리가 설정된 이후에는 대상 커밋을 명시한다.
- 수정 후 재검토는 지적 사항과 수정 범위에 집중한다. 관련 근거 없이 전체 검사를 반복하지 않는다.
- 실제 API 대신 모의 응답을 기본으로 사용한다. 코드 실행·제출에서 API 호출이 0회인지 검증한다.
- 최종 통합 단계에서 언어별 실행, API 연결, 데이터 보존, 격리, 화면 흐름을 함께 확인한다.
- 긴 로그와 상세 분석은 작업 기록으로 남기고, 총괄에는 상태·변경·검증·쟁점만 짧게 보고한다.
- 완료 보고에는 실제 수행한 검사와 미검증 항목을 구분한다.

## 문서 관리

- PLAN.md: 제품 설계, 기술 선택, 데이터 계약, 구현 단계, 인수 기준의 기준 문서.
- AGENTS.md: 팀 역할, 모델 배정, 작업 범위, 위임·검토 규칙의 기준 문서.
- CLAUDE.md: Claude가 위 두 문서를 읽도록 안내하는 진입 문서.
- 같은 설계와 지침을 세 파일에 반복 복사하지 않는다. 각 문서의 담당 내용만 수정하고 상대 링크를 유지한다.
