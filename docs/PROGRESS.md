# 구현 및 검증 기록

기준일: 2026-09-17. 사용자가 구현을 승인한 로컬 웹앱이며, 공개 배포나 실제 유료 API 호출은 수행하지 않았다.

## 현재 상태

- Docker Desktop 설치·실행 확인: macOS Apple Silicon, Docker 29.8.0, Compose 5.5.1.
- C++17·Python 3.12·Java 21·JavaScript(Node.js 22) 채점, DB, API, 프런트엔드 구현 및 최종 저장·화면 전환 보완 완료.
- `docker compose up --build -d`로 서버·실행기·웹을 구동했다. 접속 주소는 `http://localhost:8080`이며 API와 실행기는 호스트 포트를 노출하지 않는다.
- `.env`는 예제로부터 생성했고 API 키는 비워 두었다. 개인 데이터와 로컬 도구·작업 보고서는 Git에서 제외한다.

## 팀 운영

- 공통 계약을 총괄이 관리하고, 운영 코드 구현 담당은 한 번에 한 명만 배정했다. 구현과 독립 검토·통합 검증을 병행했다.
- 실제 구현: 백엔드·프런트엔드는 `gpt-5.6-sol` medium, 실행기는 `gpt-5.6-sol` high. 기존 담당자를 수정 작업에 재사용했다.
- 독립 검토는 `gpt-5.6-sol` medium이 담당했다. 최종 별도 `gpt-6-astra` 에이전트는 세션의 에이전트 한도 때문에 추가하지 못했으며, 총괄이 실제 Docker·브라우저 통합 검증을 수행했다.
- 최초 폴더에 계획 문서만 있어 현재 폴더에서 Git을 초기화했다. GitHub 원격 등록·푸시는 수행하지 않았다.

## 구현 및 검토 결과

### 백엔드

- `8ec4401`: 문제·DB·작업·AI API 초기 구현, 16개 테스트 통과.
- `45832eb`: 원문 예제·사용자 입력 검증, 엄격한 검증기 반환값, PATCH 필드 검증, 제출 코드 조회, Java 클래스·JavaScript 템플릿, 요청별 AI 클라이언트. 30개 테스트 통과.
- `92c9898`: 테스트 변경과 생성 결과 설치의 원자적 버전 검사, 공개 예제 생성, UTC 응답 시각, 구조화 출력 계약 및 AI 프롬프트 보완. 38개 테스트, compileall, OpenAPI 내보내기 통과. 수정 범위 독립 재검토에서 추가 중요 지적 없음.

### 실행기

- `aae1d18`: 네 언어 실행 래퍼·격리·자원 제한 구현, 실제 Docker 테스트 37개 통과.
- `c25fbe7`: 입력 쓰기 중에도 실행 제한 유지, 사용자 로그와 내부 결과 프레임 크기 분리, 실패 경로 이미지 정리, Java 메모리 판정 보완. 실제 Docker 테스트 44개 통과.
- `4e8bc28`: Java가 임의로 출력한 오류 문구를 메모리 초과로 오판하지 않도록 명시적 래퍼 신호 사용. 실제 Java 회귀 테스트 2개와 관련 단위 테스트 21개 통과. 수정 범위 독립 재검토에서 추가 지적 없음.
- 실행 컨테이너에는 네트워크·호스트 디렉터리·API 키·DB·Docker 소켓·기대 정답이 없다. 비루트·읽기 전용 파일시스템 및 CPU·메모리·프로세스·시간·출력량 제한을 적용한다.
- 측정치는 참고용 실행 정보이며, 실제 제한은 실행 관리자와 Docker가 적용한다. 기대값 비교는 백엔드가 수행한다.

### 프런트엔드와 로컬 구동

- `f87acdc`: 문제 목록·이미지/텍스트 등록·문제 편집·Monaco·테스트/결과/기록·요청형 도우미 구현. 8개 테스트·타입 검사·빌드 통과.
- `bc5d0ca`: 같은 화면 내 저장 순서, 초기 조회의 문제 식별, 도우미 실패 입력 보존, 미저장 공개 예제 처리, 공개 테스트 종류, Compose 빌드 경로 보완. 13개 테스트·타입 검사·빌드 통과.
- `32d0ad1`: 내부 네트워크에만 연결된 웹 컨테이너에서 localhost 포트가 열리지 않는 현상을 재현한 뒤 웹 전용 브리지 네트워크 추가. 실제 `127.0.0.1:8080` 바인딩과 HTTP 200 확인.
- `d291086`: 문제·언어별 저장 큐, 재진입 전 실패 저장 재시도, 편집/실행/도우미 응답의 문제·작업 식별, 삭제 실패 처리, StrictMode 저장 상태, Monaco 너비 수정. 21개 테스트 통과.
- `5cb180d`: 지연된 저장이 끝난 뒤 이전 화면의 실행을 시작하지 않도록 차단하고, 문제별 도우미 입력을 분리했다. 최종 24개 Vitest 테스트·타입 검사·프로덕션 빌드 통과. 총괄이 수정 범위를 검토하고 실제 브라우저 회귀 검증을 수행했다.
- 최종 커밋으로 `docker compose up --build -d --no-deps web` 성공. 개발용 호스트 API·실행기·Vite는 종료하고 Docker 서비스만 실행해 두었다.

## 총괄의 실제 통합 검증

### Compose 서비스와 데이터

- 최신 백엔드·실행기 컨테이너를 통한 네 언어 정답 제출 각각 5/5, 오답 실행 각각 1/3 판정 확인. 0을 반환하는 오답은 합계가 0인 공개 예제만 통과한다.
- 실행/제출 중 AI 사용량 변화 없음. 히든 응답에서 입력·기대값·실제값·표준 출력/오류가 제외됨을 확인했다.
- 서버를 `--force-recreate`로 재생성한 뒤 문제·공개 테스트·JavaScript 초안·8개 실행 기록이 유지됨을 확인했다. 검증 전용 데이터는 식별자로 한정해 정리했다.
- 공개/사용자 테스트 생성 허용, 외부 히든 생성 요청 422, UTC `Z` 응답을 실제 HTTP로 확인했다.
- 최종 실행 중인 서버·실행기의 Python 파일 해시가 작업 폴더와 일치함을 확인했다. 임시 실행 이미지가 남지 않았고 `http://localhost:8080/api/health`가 DB·Docker 정상 상태를 반환한다.

### AI 모의 응답과 실제 Docker 생성 파이프라인

- 임시 DB와 AI 모의 응답을 사용하고, 생성기·검증기·기준 풀이·단순 풀이 실행은 모두 실제 Docker에서 수행했다.
- 원문 예제 및 작은 입력 50개를 교차 검증한 뒤 공개 3개·사용자 1개·히든 28개를 설치했다. 후보 중 공개 테스트를 보충하고 중복을 제외하므로 히든 수는 목표보다 적을 수 있다.
- JavaScript 실행 4/4, 제출 32/32 통과. 기대값 계산과 제한사항 검증도 확인했다.
- 생성 후 실행·제출·기대값 계산에서는 추가 AI 호출이 없었다. 임시 DB는 검증 후 제거했다.

### 실제 브라우저

- 번들에 포함된 Monaco 에디터와 한국어 화면, 외부 CDN 요청이 없는 구성을 확인했다.
- JavaScript 초안 자동 저장·새로고침 복원, 실행 3/3·제출 5/5, 제출 소스 조회를 확인했다.
- 사용자 테스트 추가, 기대값 계산 후 명시적 적용, 실행 4/4, 삭제를 확인했다.
- 직접 문제 등록·공개 예제 추가/편집·미저장 예제의 생성 차단을 확인했다.
- API 키가 없는 분석·생성·도우미 요청의 503 안내와 입력 보존, 전체 정답 요청의 명시적 선택을 확인했다. 실제 유료 API는 호출하지 않았다.
- 이미지 단독 업로드의 요청 본문과 API 실패 후 선택 파일 보존을 확인했다.
- 최종 Docker 웹에서 JavaScript 자동 저장·제출 5/5, 히든 결과 비공개, 1440→390px 화면 축소 후 가로 넘침 없음, 외부 에셋 요청·브라우저 예외 없음 확인.
- 지연된 저장 후 문제 전환을 실제 브라우저에서 재현했다. 수정 전에는 이전 문제 실행 요청이 발생했고, `5cb180d`에서는 요청이 발생하지 않았다. 이전 도우미 요청이 실패해도 새 문제의 입력이 유지됨을 확인했다.
- 모든 브라우저 검증에서 실제 AI 사용량 변화 없음. 검증 전용 문제·케이스·작업은 생성한 식별자로 한정해 제거하고 원래 초안을 복원했다.

## 후속 수정: 출력창과 문제 섹션

- 사용자 요청에 따라 출력창의 문자 그대로 표시되던 `stdout\n`·`stderr\n` 접두사를 제거했다. 제목을 `출력`·`오류 출력`으로 분리하고 실제 로그의 줄바꿈·리터럴 백슬래시는 보존한다.
- 왼쪽 문제 영역을 문제 설명 → 제한사항 → 입출력 예 → 입출력 예 설명 순서로 변경했다. 공개 예제만 매개변수 이름과 `result` 열의 표로 표시하며 함수 정보는 접을 수 있다.
- `example_explanation` Markdown 필드를 DB·API·AI 분석/생성/도우미 컨텍스트에 추가했다. 편집 화면에서 저장할 수 있고 Markdown 표를 지원한다. 원문에 없는 설명은 만들지 않는다.
- `32ec97c`: 서버 테스트 41개·컴파일 검사 통과. `f977f82`: 웹 테스트 28개·타입 검사·프로덕션 빌드 통과. 총괄이 변경 범위를 검토했다.
- SQLite 온라인 백업의 무결성을 확인한 뒤 서버를 업데이트했다. 기존 DB의 테스트와 작성 코드가 유지되고 새 필드가 빈 문자열로 추가됨을 실제 API로 검증했다.
- 최신 Docker 서버·웹 빌드 및 구동 성공. 실제 브라우저에서 섹션 순서, 공개 예제만 표시, 설명 편집·저장·새로고침 복원, Markdown 표, Python 실행 3/3 및 stdout/stderr 원문 보존, 390px 가로 넘침 없음 확인. AI 호출과 브라우저 예외는 없었다.
- 검증 전용 문제와 관련 기록만 생성한 식별자로 제거했다. 사용자 스크린샷은 수정·커밋하지 않았다.

## 후속 수정: AI 풀이 반환 보정과 풀이 완료 표시

- `8198a91`: 공개 예제의 반환 타입 오류·기대값 불일치에 한해 보정 규칙을 AI에 최대 한 번 요청한다. 서버가 두 원본 풀이를 보존한 동일한 반환 래퍼를 생성하고, 모든 준비 검증을 다시 수행한다. 기대값·사용자 코드는 변경하지 않는다.
- Python 실행기의 내부 `return_type` 표식으로 반환 타입 오류를 일반 예외·시간·메모리·출력 제한과 구분한다. 원본 값과 표식은 공개 제출 응답에 노출하지 않는다.
- `9888887`: 성공한 전체 제출 기록을 바탕으로 `is_solved`를 조회한다. 실제 JSON true 및 양의 정수 테스트 수를 요구하고, run·실패·중단·0건은 제외한다. 과거 성공과 이후 실패·서버 재시작에도 완료 기록을 유지한다. 목록은 두 번의 일괄 조회를 사용한다.
- `d860520`: 독립 검토에서 확인한 지원 불가 타입의 불필요한 추가 API 호출과 미완성 응답의 사용량 누락을 수정했다. 수정 범위 재검토에서 추가 중요 지적 없음. 최종 서버 테스트 66개, 출력 보정 추가 당시 서버·실행기 94개(실제 Docker opt-in 18개 제외), 실제 Docker 집중 회귀 2개 통과.
- 총괄이 별도 임시 DB와 모의 AI 응답을 최신 Docker 실행기에 연결했다. 배열 `[3, 2, 2, 3, 1]`을 반환하는 두 풀이가 `32231`로 보정되고, 작은 입력 50개 교차 검증을 거쳐 공개 3개·히든 28개가 설치됐다. 원래 공개 기대값은 유지됐다.
- 실제 C++·Python·Java·JavaScript 실행 각각 3/3, Python 제출 31/31 통과. 실제 오답 실행·제출은 0건 통과이며, 히든 응답 비공개·저장된 기준 풀이로 기대값 계산·성공 제출 후 완료 표시·이후 실패 및 앱 재시작 후 완료 유지도 확인했다. 모의 AI 호출은 생성 1회와 보정 규칙 1회뿐이며 채점 중 추가 호출은 없었다.
- 실제 유료 AI 요청과 사용자 DB 수정 없이 검증했다. 임시 컨테이너/DB는 검증 후 제거됐다. 사용자 DB는 변경 전 온라인 백업의 무결성을 확인했다.
- `cfd1878`: OpenAI SDK에서 출력 토큰 한도가 추론 토큰을 포함함을 확인하고, 보정 요청의 한도를 600에서 4000으로 조정했다. 모델별 추론 옵션을 강제하지 않으며 호출 횟수는 최대 한 번으로 유지한다. SDK 모의 계약 검사 2개 통과.
- `54f0b38`·`a991096`: 문제 편집의 보정 안내·적용 규칙, 목록/풀이 화면의 완료 배지 및 완료 필터를 추가했다. 제출 후 문제 상태와 제출 기록을 독립 조회하여 한 조회가 실패해도 다른 성공 결과를 반영한다. 이전 경로의 늦은 응답이 현재 화면을 덮지 않음을 검증했다. 최종 웹 테스트 42개·타입 검사·프로덕션 빌드 통과.
- 최신 Docker 웹에서 실제 브라우저로 보정 안내·새로고침 유지·실패 후 재시도, run 성공 시 완료 미표시, submit 후 완료 표시, 재조회·목록 필터, 390px 화면의 완료 표시와 가로 넘침 없음을 확인했다. 문제/제출 응답은 별도 UI fixture로 검사했으며, 실제 저장 DB의 목록에서도 완료 배지 1개와 필터 결과가 API 집계와 일치함을 확인했다. 브라우저 예외와 실제 AI 요청은 없었다.
- `60561dd`: 반환 타입 오류를 내부 표식으로 분류하면서도 사용자에게 타입 오류 설명을 유지하도록 보완했다. 실제 Docker에서 오류 설명이 사라지는 실패를 재현한 뒤 수정했으며 해당 회귀 1개와 실행기 단위 검사 24개 통과.
- 기존 사용자 DB 2개 문제의 전체 덤프 해시가 배포 전 온라인 백업과 동일함을 확인했다. 새 완료 필드와 보정 필드가 실제 API에 제공되고, 기존 성공 제출이 별도 데이터 변경 없이 완료 상태로 반영된다. 사용자 스크린샷은 수정·커밋하지 않았다.

## 미검증 범위

- 실제 OpenAI API 응답의 품질·지연·과금. 모의 응답과 실패 처리는 검증했다.
- Windows 실기기. Docker Desktop Linux 컨테이너용 설정과 PowerShell 안내를 제공한다.
- 원본 코딩 사이트의 공식 채점과 동일한 판정/점수. 이 앱의 생성된 테스트 통과만 의미한다.

상세 로컬 로그와 화면 캡처는 Git에서 제외한 `docs/work/`에 보관한다.
